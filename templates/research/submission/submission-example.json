{
  "claims": [
    {
      "question": "What accuracy does the SVM classifier achieve on MNIST test set?",
      "answer": "0.9234 (subsample-trained on 5000 examples per CONFIG.MNIST.SVM_TRAIN_SUBSET)"
    },
    {
      "question": "What is the most-confused digit pair in the confusion matrix?",
      "answer": "4 misclassified as 9 (47 errors), then 7 misclassified as 1 (38 errors)"
    },
    {
      "question": "What is the dimensionality of the UMAP projection used for visualization?",
      "answer": "2D (n_components=2, default per scripts/mnist/03_plot_umap_space.py)"
    }
  ],
  "notes": "This is a shape primer for cohort-A SciTeX/Clew submissions. Each `question` + `answer` MUST be a string; numbers / lists / bools should be serialized to string form. Extra keys per claim (`evidence`, `figure_path`, ...) are allowed and ignored by the validator. Top-level `notes` is optional."
}
