# Runtime dependencies for the clew-aware MNIST example.
# `pip install -r requirements.txt` or `uv pip install -r requirements.txt`.
scitex>=2.0
scitex-clew>=0.2
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
torch>=2.0
torchvision>=0.15
umap-learn>=0.5
